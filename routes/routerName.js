// update filename to name of router in question (i.e. "usersRouter.js")
import { Router } from "express";
// import controller (i.e. `import * as usersController from "../controllers/usersController.js"`)

// const [routerName] = Router();

// set up routes, i.e. :
// [routerName].get("/", [controllerName].[controllerFunction]);
//
// etc.

// export default [routerName]
// or 
// export { [routerName] }
